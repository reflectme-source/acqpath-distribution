{
  "schema": "acqpath.integration.examples.v1",
  "synthetic": true,
  "notExecuted": true,
  "quoteInput": {
    "resource": "https://rslstandard.org/",
    "purpose": "ai-input",
    "user_class": "commercial",
    "tier": "fresh",
    "max_total_micro": "20000"
  },
  "resourceCaveat": "The root URL returned LICENSE_REQUIRED in the 2026-09-11 unpaid production check. Recheck current availability; example output remains synthetic and no paid mainnet E2E is claimed.",
  "unavailable": {
    "available": false,
    "reason": "NO_VERIFIABLE_DECLARATION",
    "charge_micro": "0"
  },
  "decisions": [
    "ALLOW_DECLARED",
    "DENY_DECLARED",
    "LICENSE_REQUIRED",
    "UNKNOWN"
  ],
  "privateFieldsOmitted": [
    "claim_token",
    "payment signatures",
    "checkpoint",
    "wallet credentials"
  ],
  "workflow": {
    "when": "Before selected supported content enters the intended workflow",
    "input": "Canonical HTTPS URL plus actual purpose/context and bounded budget",
    "unknown": "Hold; do not treat as permission",
    "purchase": "POST /v1/rights/preflight -> verified offer -> unchanged official x402 signer -> AcqPath SIWX adapter -> verified report",
    "verification": "Pinned Ed25519 offer/report/receipt/delivery binding via reviewed buyer client",
    "retry": "Same operation ID, input and private directory; reuse saved payment/SIWX, never replace a pending authorization",
    "duplicatePrevention": "One logical ID, local lock, checkpoint before submission, same authorization on retry",
    "integrationGuide": "https://developers.getacqpath.com/examples/OFFICIAL-CLIENTS.md"
  },
  "mcp": {
    "generic": {
      "mcpServers": {
        "acqpath": {
          "url": "https://api.getacqpath.com/mcp"
        }
      }
    },
    "vscode": {
      "servers": {
        "acqpath": {
          "type": "http",
          "url": "https://api.getacqpath.com/mcp"
        }
      }
    }
  }
}
